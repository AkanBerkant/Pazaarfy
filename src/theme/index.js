import { colors, textColors } from './colors';
import { commonStyles } from './styles';
import {
  fonts,
  fontSize,
  height,
  radius,
  shadow,
  sizes,
  spacing,
  textStyles,
} from './theme';

export {
  colors,
  commonStyles,
  fonts,
  fontSize,
  height,
  radius,
  shadow,
  sizes,
  spacing,
  textColors,
  textStyles,
};
